# 📱 股票条件监听 App（Flutter 版）

这是一个用 **Flutter** 开发的 Android 股票条件监听应用，支持自选股管理、信号查询和条件触发提醒。

> 💡 **本文档适合完全没有开发经验的朋友阅读**，每一步都有详细说明，按顺序操作即可。

---

## 目录

1. [什么是 Flutter？](#1-什么是-flutter)
2. [第一步：安装必要软件](#2-第一步安装必要软件)
3. [第二步：下载项目代码](#3-第二步下载项目代码)
4. [第三步：安装项目依赖](#4-第三步安装项目依赖)
5. [第四步：运行和调试](#5-第四步运行和调试)
6. [第五步：打包成 APK 安装包](#6-第五步打包成-apk-安装包)
7. [项目结构说明](#7-项目结构说明)
8. [常见报错解决方法](#8-常见报错解决方法)

---

## 1. 什么是 Flutter？

Flutter 是 Google 推出的开发框架，**只写一套代码，就能同时生成 Android 手机 App、iPhone App、网页、Windows 桌面程序**。

本项目使用 Flutter 开发，主要目标是 Android 手机 App。

---

## 2. 第一步：安装必要软件

> ⚠️ 这一步只需要做一次，以后都不用重复。

### 2.1 安装 Flutter SDK

1. 打开网址：https://flutter.dev/docs/get-started/install/windows
2. 点击 **"Download Flutter SDK"** 下载压缩包（文件名类似 `flutter_windows_3.x.x-stable.zip`）
3. 解压到一个**没有中文、没有空格**的路径，例如 `C:\flutter`（❌ 不要放到桌面或中文目录）
4. 把 Flutter 添加到系统环境变量：
   - 右键「此电脑」→「属性」→「高级系统设置」→「环境变量」
   - 在「系统变量」里找到 `Path`，双击编辑
   - 点「新建」，输入 `C:\flutter\bin`（根据你实际解压路径填写）
   - 一路点「确定」保存

5. 打开「终端」（按 `Win + R`，输入 `powershell`，回车），输入以下命令验证：

```
flutter --version
```

如果显示版本号，说明安装成功 ✅

### 2.2 安装 Android Studio（用于安装 Android 工具和模拟器）

1. 下载地址：https://developer.android.com/studio
2. 下载后双击安装，一路点「Next」，保持默认设置即可
3. 安装完成后打开 Android Studio，它会自动下载 Android SDK（需要等待几分钟）

### 2.3 接受 Android 开发许可协议

打开终端，运行：

```
flutter doctor --android-licenses
```

一直输入 `y` 并回车，直到结束。

### 2.4 检查环境是否全部正常

```
flutter doctor
```

运行后会看到一个检查结果列表，带 ✅ 的说明正常，带 ❌ 的说明还有问题。

> 只要 **Flutter** 和 **Android toolchain** 这两项是 ✅ 就可以正常开发了。

### 2.5 安装 VS Code（代码编辑器，推荐）

1. 下载地址：https://code.visualstudio.com/
2. 安装完成后打开 VS Code，在左侧「扩展（Extensions）」里搜索并安装：
   - **Flutter**（同时会自动安装 Dart）

---

## 3. 第二步：下载项目代码

### 方式一：用 Git 克隆（推荐）

打开终端，进入你想存放项目的文件夹，例如 `D:\MyProject`：

```
cd D:\MyProject
git clone https://github.com/你的账号/stock_trade_flutter.git
cd stock_trade_flutter
```

### 方式二：直接下载 ZIP

在 GitHub 项目页面点击绿色的 **「Code」** 按钮 → **「Download ZIP」**，解压到本地即可。

---

## 4. 第三步：安装项目依赖

进入项目目录后，运行以下命令下载所有依赖包（类似安装 App 需要的零件）：

```
flutter pub get
```

看到 `Got dependencies!` 字样即为成功 ✅

如果项目使用了 JSON 数据模型代码生成（本项目有用），还需要运行：

```
dart run build_runner build --delete-conflicting-outputs
```

---

## 5. 第四步：运行和调试

### 方式 A：在浏览器中预览界面（最简单，无需手机）

```
flutter run -d chrome --web-port 8080
```

浏览器会自动打开，显示 App 界面。

**👉 模拟手机屏幕的技巧：**
1. 按 `F12` 打开开发者工具
2. 点击工具栏上的 📱 手机图标（Toggle Device Toolbar）
3. 顶部下拉框选择手机型号，例如「Pixel 7」或「iPhone 14 Pro」
4. 现在看到的就是真实手机比例的界面了！

### 方式 B：在 Windows 桌面窗口运行（启动最快）

```
flutter run -d windows
```

### 方式 C：用 Android 模拟器运行（最接近真实手机效果）

**第一步：在 Android Studio 创建模拟器**

1. 打开 Android Studio
2. 点击右上角 **「Device Manager」**（设备管理）
3. 点 **「Create Device」**（创建设备）
4. 选择手机型号，例如「Pixel 7」，点「Next」
5. 选择 Android 系统版本（推荐选带 ▶ 的最新版），下载后点「Next」→「Finish」

**第二步：启动模拟器**

在 Android Studio 中点击绿色三角形 ▶ 启动模拟器，等待模拟器完全开机。

**第三步：在终端运行 App**

```
flutter devices           # 查看已连接的设备列表，找到模拟器的 ID
flutter run               # 自动部署到已连接设备
```

### 方式 D：在真实 Android 手机上运行

1. 手机进入「设置」→「关于手机」→ 连续点击「版本号」7 次，开启开发者模式
2. 回到设置找到「开发者选项」，打开「USB 调试」
3. 用数据线连接手机和电脑
4. 手机上弹出「允许 USB 调试？」提示，点「允许」
5. 终端运行：

```
flutter devices    # 确认手机已被识别
flutter run        # 部署到手机
```

---

### 调试时常用快捷键

运行 `flutter run` 之后，在**终端窗口**输入以下字母：

| 按键 | 功能 | 什么时候用 |
|------|------|-----------|
| `r` | **热重载** — 保存代码后立即刷新界面，不重启 App | 改了界面样式、文字等 |
| `R` | **热重启** — 完整重启 App，状态会清空 | 改了初始化逻辑时 |
| `p` | 显示/隐藏布局网格线 | 检查元素对齐时 |
| `i` | 打开 Widget Inspector | 点击界面元素查看是哪个控件 |
| `q` | 退出运行 | 结束调试 |

### DevTools 可视化调试工具

运行后终端会打印一行地址，类似：

```
The Flutter DevTools debugger and profiler on Chrome is available at:
http://127.0.0.1:xxxxx/xxxxx=/devtools/
```

复制这个地址粘贴到浏览器打开，可以使用：

- 🔍 **Widget Inspector** — 点击 App 界面上的任意元素，查看它对应的代码位置
- 📊 **Performance** — 检测界面是否卡顿
- 🌐 **Network** — 查看所有网络请求和服务器返回的数据
- 📋 **Logging** — 查看 `print()` 输出的日志信息

---

## 6. 第五步：打包成 APK 安装包

### 打包测试用 APK（Debug 版）

```
flutter build apk --debug
```

打包完成后文件在：
```
build\app\outputs\flutter-apk\app-debug.apk
```

把这个文件发给手机，直接安装即可测试。

---

### 打包正式发布 APK（Release 版）

> ⚠️ 发布正式版本前，必须先配置「签名」，否则无法安装或上传应用市场。

**第一步：生成签名文件（只需做一次）**

在项目目录下打开终端，运行（把 `your_name` 改成你的名字拼音）：

```
keytool -genkey -v -keystore android\app\upload-keystore.jks -keyalg RSA -keysize 2048 -validity 10000 -alias upload
```

按提示填写：
- 密钥库口令（密码，自己设，记住它）
- 姓名、城市、组织等信息（随意填写）

**第二步：创建签名配置文件**

在 `android\` 目录下新建文件 `key.properties`，内容如下（替换成你自己的信息）：

```properties
storePassword=你设置的密钥库密码
keyPassword=你设置的密钥库密码
keyAlias=upload
storeFile=../app/upload-keystore.jks
```

> ⚠️ 这个文件包含密码，**不要**上传到 GitHub！（项目的 `.gitignore` 已经排除了它）

**第三步：打包**

```
flutter build apk --release
```

打包完成后文件在：
```
build\app\outputs\flutter-apk\app-release.apk
```

---

### 打包 Web 版（可以部署到网站）

```
flutter build web
```

输出在 `build\web\` 目录，把整个文件夹上传到任意静态服务器即可访问。

---

## 7. 项目结构说明

```
stock_trade_flutter/
├── lib/                        # 所有 Dart 代码都在这里
│   ├── main.dart               # 程序入口，App 从这里启动
│   ├── bootstrap/              # 启动前的初始化配置
│   ├── core/                   # 底层工具（网络请求、本地存储、常量定义、错误处理）
│   ├── data/                   # 数据层（负责跟服务器通信，处理 JSON 数据）
│   │   ├── datasources/        # 远程 API 数据源
│   │   ├── models/             # 数据模型（定义数据结构）
│   │   └── repositories/       # 数据仓库（整合多个数据源）
│   ├── domain/                 # 业务逻辑层（定义接口规范）
│   ├── presentation/           # 界面层（所有页面和 UI 组件）
│   │   ├── pages/              # 各功能页面（首页、自选股、信号页等）
│   │   ├── routes/             # 页面跳转路由配置
│   │   ├── state/              # 全局状态管理（Riverpod）
│   │   └── view_models/        # 每个页面的数据逻辑
│   └── shared/                 # 公共组件（多个页面共用的 Widget）
├── android/                    # Android 原生配置文件（一般不需要改动）
├── test/                       # 自动化测试代码
├── pubspec.yaml                # 项目配置文件（依赖包在这里声明）
└── README.md                   # 就是你正在看的这个文件
```

---

## 8. 常见报错解决方法

### ❌ `flutter` 不是内部命令

**原因：** Flutter 没有正确添加到环境变量。

**解决：** 重新检查第 2.1 节的环境变量配置，添加后**关闭终端重新打开**再试。

---

### ❌ `This application is not configured to build on the web`

**原因：** 项目缺少 Web 平台支持文件。

**解决：**
```
flutter create .
```

---

### ❌ 运行报错 `Could not find a file named "pubspec.yaml"`

**原因：** 终端当前不在项目目录里。

**解决：** 先进入项目目录：
```
cd D:\MyProject\stock_trade_flutter
```

---

### ❌ 编译报错 `*.g.dart` 文件找不到

**原因：** JSON 序列化代码没有生成。

**解决：**
```
dart run build_runner build --delete-conflicting-outputs
```

---

### ❌ 依赖包下载失败（网络超时）

**原因：** Flutter 默认从国外服务器下载，国内网络可能较慢。

**解决：** 在终端设置国内镜像源（每次打开新终端都要设置，或者写入系统环境变量）：
```
$env:PUB_HOSTED_URL="https://pub.flutter-io.cn"
$env:FLUTTER_STORAGE_BASE_URL="https://storage.flutter-io.cn"
flutter pub get
```

---

### ❌ 手机连接后 `flutter devices` 看不到设备

**解决步骤：**
1. 确认手机已开启「开发者选项」和「USB 调试」
2. 换一根数据线（部分线只能充电不能传输数据）
3. 手机弹出「USB 连接方式」时选择「文件传输（MTP）」
4. 重新运行 `flutter devices`

---

## 相关资源链接

| 资源 | 地址 |
|------|------|
| Flutter 官方文档 | https://docs.flutter.dev/ |
| Flutter 中文文档 | https://flutter.cn/ |
| Riverpod 状态管理 | https://riverpod.dev/ |
| Dio 网络请求库 | https://pub.dev/packages/dio |
| DevTools 使用指南 | https://docs.flutter.dev/tools/devtools/overview |
| Flutter 视频教程（B站） | 搜索「Flutter 入门」 |
